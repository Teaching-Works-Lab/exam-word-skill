interface:
  display_name: "机械学院试卷 Word"
  short_description: "对比、检查、套版并验证机械学院标准试卷 Word"
  default_prompt: "Use $mechanical-exam-word to compare or standardize the attached exam files without overwriting the originals."
policy:
  allow_implicit_invocation: true
